{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Materiels</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Desingation</th>
                <td>{{ entity.desingation }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('materiels') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('materiels_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
